extends layout

block content
  h1= title
  p Welcome to #{title}
  if user
    p #{user}
    p
      a(href="/secure/app/") go to the app
    p
      a(href="/logout") logout
  else
    p not logged in
    p
      a(href="/auth/google") authenticate with google
